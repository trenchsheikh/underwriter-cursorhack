# Frontend

React + Vite + TypeScript app for underwriter-cursorhack.

## Setup

```bash
npm install
```

## Run

```bash
npm run dev
```

Dev server: http://localhost:5173

Requests to `/api/*` are proxied to the backend at `http://localhost:8000`.

## Build

```bash
npm run build
npm run preview
```
