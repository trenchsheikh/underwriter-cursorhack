# AGENTS.md

## Cursor Cloud specific instructions

This is an empty starter repository (`underwriter-cursorhack`). It contains only a template `README.md` — no application code, dependencies, or services exist yet.

- **No build/test/lint commands** are available until project code is added.
- **No services** need to be started.
- **Python 3.12** and **Git 2.43** are available in the base environment.
- When application code is added, update this file with service startup instructions, test commands, and any non-obvious caveats.
